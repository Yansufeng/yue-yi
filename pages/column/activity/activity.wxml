<!--pages/column/activity/activity.wxml-->
<searchbar />
<!-- 获取tab栏组件修改 -->
<tabbar tabs="{{ tabs }}" bind:tabEvent="tabChange" />

<view class="cards">
  <view
    class="card"
    wx:for="{{ shows }}"
    wx:key="id"
  ></view>
</view>
